// find smallest no of them.

#include<iostream>
using namespace std;
int  main()
{
    int a,b;
    cout<<"Enter a: ";
    cin>>a;
    cout<<"Enter b: ";
    cin>>b;
    if(a>b)
    {
        cout<<b<<" is smallest number";
    }
    else
    {
        cout<<a<<" is smallest number";
    }
}